trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      Write-Host "This is the predefined variablefor build number:"
      Write-Host "$(Build.BuildNumber)"
